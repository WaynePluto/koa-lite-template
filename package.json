{
  "name": "koa-server",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "tsx watch src/app.ts",
    "_build": "npm run clear && esbuild src/app.ts --sourcemap --bundle --outdir=dist --platform=node --minify",
    "build": "npm run clear && node esbuild.config.js",
    "start": "node --enable-source-maps ./dist/app.js",
    "clear": "rimraf dist"
  },
  "dependencies": {
    "knex": "^2.4.2",
    "koa": "^2.14.1",
    "mysql2": "^3.2.0",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/koa": "^2.13.5",
    "@types/node": "^18.14.5",
    "esbuild": "^0.17.10",
    "koa-compose": "^4.1.0",
    "rimraf": "^4.3.0",
    "tsx": "^3.12.3",
    "typescript": "^4.9.5"
  }
}